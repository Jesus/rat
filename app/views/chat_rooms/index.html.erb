<h1>🐀</h1>

<div id="messages"></div>

<%= form_for :message, { url: '#', html: { id: 'new_message' } } do |f| %>
  <div class="form-group">
    <%= f.label :body %>
    <%= f.text_field :body, class: 'form-control' %>
  </div>

  <%= f.submit "Post", class: 'btn btn-primary btn-lg' %>
<% end %>
